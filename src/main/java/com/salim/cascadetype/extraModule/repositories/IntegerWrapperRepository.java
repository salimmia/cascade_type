package com.salim.cascadetype.extraModule.repositories;

import com.salim.cascadetype.extraModule.IntegerWrapper;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IntegerWrapperRepository extends JpaRepository<IntegerWrapper, Integer> {
}
