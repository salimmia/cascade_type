package com.salim.cascadetype.extraModule.repositories;

import com.salim.cascadetype.extraModule.StringWrapper;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StringWrapperRepository extends JpaRepository<StringWrapper, String> {
}
